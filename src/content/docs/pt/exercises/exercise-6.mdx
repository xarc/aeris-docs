---
title: Exercício 6
description: Consolidar o uso de laços for em Assembly RISC-V usando slti e beq.
---

import RiscVExercise from '../../../../components/RiscVExercise.astro';

## Objetivo

Consolidar o aprendizado da linguagem Assembly RISC-V e compreender como as instruções de desvio condicional são executadas dentro de um laço. O seguinte laço `for` em C é traduzido para assembly usando `slti` e `beq`:

```c
for (i = 0; i < 3; i++)
    j++;
```

## Instruções

1. Abra o simulador de linguagem RISC-V.
2. No editor de texto do simulador, transcreva o código abaixo:

```asm
# Exercício 06
# C expression: for (i = 0; i < 3; i++) j++;

    .text
main:
    addi s0, zero, 0     # i = 0

Loop:
    slti t0, s0, 3       # se i < 3 então t0 = 1, senão t0 = 0
    beq  t0, zero, Exit  # se t0 == 0, pula para Exit
    addi s1, s1, 1       # j++
    addi s0, s0, 1       # i++
    j    Loop            # volta para o início do laço

Exit:
    nop                  # fim do programa
```

:::note
As instruções `j` e `nop` são **pseudo-instruções**. Nessas linhas, copie o valor da coluna **Basic** (a instrução real) na coluna **Instrução** e o valor da coluna **Source** na coluna **Pseudoinstrução**.
:::

## Execução Passo a Passo

Execute o programa passo a passo clicando no botão **Run One Step**. A cada instrução, preencha a tabela abaixo cada vez que o valor de um registrador for modificado.

<RiscVExercise
  lang="pt"
  pseudoInstructionColumn={true}
  registers={[
    { name: "x5", alias: "t0" },
    { name: "x8", alias: "s0 (i)" },
    { name: "x9", alias: "s1 (j)" },
  ]}
  initialValues={[
    "0x00000000",
    "0x00000000",
    "0x00000000",
  ]}
  prefilledRows={[
    {
      pc: "0x00400000",
      instruction: "addi s0 zero 0",
      pseudoInstruction: "",
      values: ["", "0x00000000", ""],
    },
  ]}
  correctRows={[
    { pc: "0x00400004", instruction: "slti t0 s0 3",   values: ["0x00000001", "", ""] },
    { pc: "0x00400008", instruction: "beq t0 zero Exit",  values: ["", "", ""] },
    { pc: "0x0040000C", instruction: "addi s1 s1 1",    values: ["", "", "0x00000001"] },
    { pc: "0x00400010", instruction: "addi s0 s0 1",    values: ["", "0x00000001", ""] },
    { pc: "0x00400014", instruction: "jal x0 -16", pseudoInstruction: "j Loop", values: ["", "", ""] },
    { pc: "0x00400004", instruction: "slti t0 s0 3",   values: ["0x00000001", "", ""] },
    { pc: "0x00400008", instruction: "beq t0 zero Exit",  values: ["", "", ""] },
    { pc: "0x0040000C", instruction: "addi s1 s1 1",    values: ["", "", "0x00000002"] },
    { pc: "0x00400010", instruction: "addi s0 s0 1",    values: ["", "0x00000002", ""] },
    { pc: "0x00400014", instruction: "jal x0 -16", pseudoInstruction: "j Loop", values: ["", "", ""] },
    { pc: "0x00400004", instruction: "slti t0 s0 3",   values: ["0x00000001", "", ""] },
    { pc: "0x00400008", instruction: "beq t0 zero Exit",  values: ["", "", ""] },
    { pc: "0x0040000C", instruction: "addi s1 s1 1",    values: ["", "", "0x00000003"] },
    { pc: "0x00400010", instruction: "addi s0 s0 1",    values: ["", "0x00000003", ""] },
    { pc: "0x00400014", instruction: "jal x0 -16", pseudoInstruction: "j Loop", values: ["", "", ""] },
    { pc: "0x00400004", instruction: "slti t0 s0 3",   values: ["0x00000000", "", ""] },
    { pc: "0x00400008", instruction: "beq t0 zero Exit",  values: ["", "", ""] },
    { pc: "0x00400018", instruction: "addi x0 x0 0", pseudoInstruction: "nop", values: ["", "", ""] },
  ]}
  totalRows={8}
/>

