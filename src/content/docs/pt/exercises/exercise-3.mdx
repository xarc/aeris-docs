---
title: Exercício 3
description: Consolidar o uso da instrução de desvio condicional em RISC-V.
---

import RiscVExercise from '../../../../components/RiscVExercise.astro';

## Objetivo

Consolidar o uso da instrução de desvio condicional em RISC-V. A seguinte expressão em C é traduzida para assembly usando `beq`:

```c
if (i != j)
    f = g + h;
f = f - i;
```

## Instruções

1. Abra o simulador de linguagem RISC-V.
2. No editor de texto do simulador, transcreva o código abaixo:

```asm
# Exercício 03
# C expression:
#   if (i != j)
#       f = g + h;
#   f = f - i;

    .text
main:
    addi s0, zero, 15     # f = 15
    addi s1, zero, 10     # g = 10
    addi s2, zero, 20     # h = 20
    addi s3, zero, 1      # i = 1
    addi s4, zero, 2      # j = 2

    beq  s3, s4, L1       # if (i == j) goto L1
    add  s0, s1, s2       # f = g + h

L1:
    sub  s0, s0, s3       # f = f - i
```

## Execução Passo a Passo - Cenário 1 (i ≠ j)

Com os valores iniciais `i = 1` e `j = 2`, o desvio **não é tomado** e todas as instruções são executadas.

Execute o programa passo a passo clicando no botão **Run One Step**. A cada instrução, preencha a tabela abaixo cada vez que o valor de um registrador for modificado.

<RiscVExercise
  lang="pt"
  registers={[
    { name: "R8",  alias: "s0" },
    { name: "R9",  alias: "s1" },
    { name: "R18", alias: "s2" },
    { name: "R19", alias: "s3" },
    { name: "R20", alias: "s4" },
  ]}
  initialValues={[
    "0x00000000",
    "0x00000000",
    "0x00000000",
    "0x00000000",
    "0x00000000",
  ]}
  prefilledRows={[
    {
      pc: "0x00400000",
      instruction: "addi s0 zero 15",
      values: ["0x0000000F", "", "", "", ""],
    },
  ]}
  correctRows={[
    { pc: "0x00400004", instruction: "addi s1 zero 10", values: ["", "0x0000000A", "", "", ""] },
    { pc: "0x00400008", instruction: "addi s2 zero 20", values: ["", "", "0x00000014", "", ""] },
    { pc: "0x0040000C", instruction: "addi s3 zero 1",  values: ["", "", "", "0x00000001", ""] },
    { pc: "0x00400010", instruction: "addi s4 zero 2",  values: ["", "", "", "", "0x00000002"] },
    { pc: "0x00400014", instruction: "beq s3 s4 L1",     values: ["", "", "", "", ""] },
    { pc: "0x00400018", instruction: "add s0 s1 s2",    values: ["0x0000001E", "", "", "", ""] },
    { pc: "0x0040001C", instruction: "sub s0 s0 s3",    values: ["0x0000001D", "", "", "", ""] },
  ]}
  totalRows={7}
/>

## Execução Passo a Passo - Cenário 2 (i = j)

Agora altere a instrução `addi` que define `s4` para atribuir `j = 1` (igual a `i`):

```asm
    addi s4, zero, 1      # j = 1
```

Clique em **Reset** para recarregar o programa e execute passo a passo novamente. Com `i = j = 1`, o desvio **é tomado** e a instrução `add` é ignorada.

<RiscVExercise
  lang="pt"
  registers={[
    { name: "R8",  alias: "s0" },
    { name: "R9",  alias: "s1" },
    { name: "R18", alias: "s2" },
    { name: "R19", alias: "s3" },
    { name: "R20", alias: "s4" },
  ]}
  initialValues={[
    "0x00000000",
    "0x00000000",
    "0x00000000",
    "0x00000000",
    "0x00000000",
  ]}
  prefilledRows={[
    {
      pc: "0x00400000",
      instruction: "addi s0 zero 15",
      values: ["0x0000000F", "", "", "", ""],
    },
  ]}
  correctRows={[
    { pc: "0x00400004", instruction: "addi s1 zero 10", values: ["", "0x0000000A", "", "", ""] },
    { pc: "0x00400008", instruction: "addi s2 zero 20", values: ["", "", "0x00000014", "", ""] },
    { pc: "0x0040000C", instruction: "addi s3 zero 1",  values: ["", "", "", "0x00000001", ""] },
    { pc: "0x00400010", instruction: "addi s4 zero 1",  values: ["", "", "", "", "0x00000001"] },
    { pc: "0x00400014", instruction: "beq s3 s4 L1",     values: ["", "", "", "", ""] },
    { pc: "0x0040001C", instruction: "sub s0 s0 s3",    values: ["0x0000000E", "", "", "", ""] },
  ]}
  totalRows={6}
/>

Compare as duas tabelas e analise a diferença no fluxo de execução (observe a sequência de valores do **PC**) e o valor final de `s0`.

